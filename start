#/bin/sh

source .env
docker-compose start
echo "webserver running on http://localhost:${NGINX_PORT}"
echo "phpmyadmin running on http://localhost:${PMA_PORT}"
echo "remote mysql running on mysql://localhost:${MYSQL_PORT}"
